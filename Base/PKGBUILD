# Maintainer: Name Surname <email@email.com>
pkgname=helpzbase
_pkgname=libhelpzbase
pkgver=1.2.100
pkgrel=1
pkgdesc="Daidev helpz base lib"
arch=('x86_64')
license=('custom')
depends=('qt5-base')
source=("root.tar.gz")
md5sums=('SKIP') # TODO: make it?
package() {
	cd "${srcdir}"
	tar -zxf root.tar.gz -C "$pkgdir"
}
