# Maintainer: Name Surname <email@email.com>
pkgname=
pkgver=
pkgrel=1
arch=('x86_64')
depends=()

pkgdesc="Daidev Helpz package"
license=('custom')
source=("root.tar.gz")
md5sums=('SKIP') # TODO: make it?

package() {
	cd "${srcdir}"
	tar -zxf root.tar.gz -C "$pkgdir"
}
